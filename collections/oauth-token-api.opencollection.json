{
  "opencollection": "1.0.0",
  "info": {
    "name": "OAuth 2.0 Server Authorization Token API",
    "version": "1.0.0"
  },
  "items": [
    {
      "info": {
        "name": "Token",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Token Endpoint",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://authorization-server.example.com/token",
            "body": {
              "type": "form-urlencoded",
              "data": []
            },
            "auth": {
              "type": "basic",
              "username": "{{username}}",
              "password": "{{password}}"
            }
          },
          "docs": "The token endpoint is used by the client to obtain an access token by presenting its authorization grant or refresh token. Supports authorization code, client credentials, resource owner password credentials, and refresh token grant types."
        }
      ]
    }
  ],
  "bundled": true
}